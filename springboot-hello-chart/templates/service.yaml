apiVersion: v1
kind: Service
metadata:
  name: {{ include "springboot-hello-chart.fullname" . }}
  labels:
    {{- include "springboot-hello-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "springboot-hello-chart.selectorLabels" . | nindent 4 }}
